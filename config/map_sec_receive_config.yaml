# Serial port settings
serial:
  port: "/dev/ttyMAPSEC"
  baud_rate: 115200
  timeout: 1.0

# Frame protocol
frame:
  header: 0x5A
  tail: 0xA5
  frame_len: 4
  payload_len: 2

# Reconnection
reconnect:
  base_delay: 5.0
  max_delay: 5.0

# Buffer and polling
buffer_max: 2048
receive_hz: 200
